# Data Generation Configuration
data_generation:
  output_dir: "dataset/data/sudoku-4x4"
  train_size: 1000
  val_size: 200
  test_size: 200
  num_augmentations: 20
  augmentation_type: "digit_permutation"
  ensure_no_equivalents: true  # Check for equivalent puzzles within each split
  seed: 42
